apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: homepage
  name: homepage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homepage
  strategy: {}
  template:
    metadata:
      labels:
        app: homepage
    spec:
      containers:
      - image: ghcr.io/gethomepage/homepage:latest
        name: homepage
        resources: {}
status: {}
